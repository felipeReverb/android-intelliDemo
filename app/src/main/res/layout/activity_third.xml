<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ThirdActivity">

    <EditText
        android:id="@+id/montoVenta"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="43dp"
        android:ems="10"
        android:inputType="number"
        android:hint="Monto"
        android:text=""
        android:textAlignment="center"/>


    <EditText
        android:id="@+id/referenceVenta"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@id/montoVenta"
        android:layout_alignLeft="@id/montoVenta"
        android:layout_alignRight="@id/montoVenta"
        android:layout_alignStart="@id/montoVenta"
        android:layout_below="@id/montoVenta"
        android:layout_marginTop="65dp"
        android:inputType="text"
        android:hint="Reference"
        android:textAlignment="center"/>

    <Button
        android:id="@+id/imageButtonPay"
        android:layout_width="match_parent"
        android:layout_height="58dp"
        android:layout_centerHorizontal="true"
        android:background="#6644aa"
        android:text="Pagar"
        android:layout_alignParentBottom="true"/>

    <TextView
        android:id="@+id/resultVenta"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:layout_above="@id/imageButtonPay"
        android:layout_centerHorizontal="true"
        android:textSize="@android:dimen/app_icon_size"
        />


</RelativeLayout>